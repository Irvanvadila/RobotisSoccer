<?xml version="1.0"?>
<launch>
     <rosparam file="$(find processing_image)/config/hsv_param_bola.yaml" />

     <include file="$(find processing_image)/launch/main.launch" />

    
  <!-- ball detector -->
    <arg name="config_path" default="$(find processing_image)/config/ball.yaml"/>

	<node pkg="processing_image" type="deteksi_bola.py" name="ball_detector" args="" output="screen">
		<rosparam command="load" file="$(arg config_path)"/>
		<param name="yaml_path" type="string" value="$(arg config_path)"/>
<!-- 		<remap from="/ball_detector_node/image_in" to="/usb_cam_node/image_raw" /> -->
<!-- 		<remap from="/ball_detector_node/cameraInfo_in" to="/usb_cam_node/camera_info" /> -->

	</node>

</launch>

